<Window x:Class="RobloxPiano.Desktop.Views.PromptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Width="380" Height="190"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    <Border Background="{StaticResource Background.Root}" 
            BorderBrush="{StaticResource Border.Default}" 
            BorderThickness="1" 
            CornerRadius="8" 
            Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Title -->
            <TextBlock x:Name="TitleText" Grid.Row="0" Text="입력" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource Text.Primary}" Margin="0,0,0,8" />

            <!-- Message -->
            <TextBlock x:Name="MessageText" Grid.Row="1" Text="내용을 입력하세요:" FontSize="12.5" Foreground="{StaticResource Text.Secondary}" Margin="0,0,0,10" />

            <!-- Input Box -->
            <TextBox x:Name="InputBox" Grid.Row="2" Height="32" VerticalContentAlignment="Center" Margin="0,0,0,16" />

            <!-- Action Buttons -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                <Button Content="취소" Padding="14,6" Margin="0,0,8,0" IsCancel="True" Click="OnCancelClick" />
                <Button Content="확인" Style="{StaticResource AccentButtonStyle}" Padding="16,6" IsDefault="True" Click="OnOkClick" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
