<UserControl x:Class="RobloxPiano.Desktop.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="32">
        <StackPanel MaxWidth="600" HorizontalAlignment="Left">
            <TextBlock Text="Settings" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,24" />
            
            <TextBlock Text="Playback Engine" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12" />
            <Border Background="{StaticResource Surface.Elevated}" BorderBrush="{StaticResource Border.Subtle}" BorderThickness="1" CornerRadius="6" Padding="16" Margin="0,0,0,24">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <TextBlock Text="Input Method" Width="150" VerticalAlignment="Center" Foreground="{StaticResource Text.Secondary}" />
                        <ComboBox Width="200" SelectedIndex="0">
                            <ComboBoxItem Content="Windows SendInput (Win32)"/>
                            <ComboBoxItem Content="RawInput Hook"/>
                        </ComboBox>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <TextBlock Text="Base Delay (ms)" Width="150" VerticalAlignment="Center" Foreground="{StaticResource Text.Secondary}" />
                        <TextBox Width="80" Text="15" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Randomization (ms)" Width="150" VerticalAlignment="Center" Foreground="{StaticResource Text.Secondary}" />
                        <TextBox Width="80" Text="5" />
                    </StackPanel>
                </StackPanel>
            </Border>
            
            <TextBlock Text="Library &amp; Storage" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12" />
            <Border Background="{StaticResource Surface.Elevated}" BorderBrush="{StaticResource Border.Subtle}" BorderThickness="1" CornerRadius="6" Padding="16">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <TextBlock Text="Database Path" Width="150" VerticalAlignment="Center" Foreground="{StaticResource Text.Secondary}" />
                        <TextBox Width="280" Text="%APPDATA%\RobloxPianoV2\library_v2.db" IsReadOnly="True" Foreground="{StaticResource Text.Disabled}"/>
                        <Button Content="Open Folder" Margin="8,0,0,0" />
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
