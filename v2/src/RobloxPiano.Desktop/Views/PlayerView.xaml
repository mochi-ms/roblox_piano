<UserControl x:Class="RobloxPiano.Desktop.Views.PlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="120" />
            <RowDefinition Height="64" />
        </Grid.RowDefinitions>

        <!-- Song Header -->
        <Border Grid.Row="0" Background="{StaticResource Surface.Base}" BorderBrush="{StaticResource Border.Subtle}" BorderThickness="0,0,0,1" Padding="24,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="Mrs. GREEN APPLE - ライラック" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,4"/>
                    <StackPanel Orientation="Horizontal">
                        <Border Background="{StaticResource Surface.Hover}" CornerRadius="4" Padding="6,2" Margin="0,0,8,0">
                            <TextBlock Text="MIDI" FontSize="11" Foreground="{StaticResource Text.Secondary}" />
                        </Border>
                        <TextBlock Text="2 Tracks" FontSize="12" Foreground="{StaticResource Text.Secondary}" VerticalAlignment="Center" />
                    </StackPanel>
                </StackPanel>
                
                <!-- Metadata Strip -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <StackPanel Margin="0,0,24,0">
                        <TextBlock Text="Duration" FontSize="11" Foreground="{StaticResource Text.Secondary}" Margin="0,0,0,2"/>
                        <TextBlock Text="04:46" FontSize="13" FontWeight="Medium"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,24,0">
                        <TextBlock Text="BPM" FontSize="11" Foreground="{StaticResource Text.Secondary}" Margin="0,0,0,2"/>
                        <TextBlock Text="150" FontSize="13" FontWeight="Medium"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,24,0">
                        <TextBlock Text="Notes" FontSize="11" Foreground="{StaticResource Text.Secondary}" Margin="0,0,0,2"/>
                        <TextBlock Text="1,482" FontSize="13" FontWeight="Medium"/>
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="Range" FontSize="11" Foreground="{StaticResource Text.Secondary}" Margin="0,0,0,2"/>
                        <TextBlock Text="A#2 – F#6" FontSize="13" FontWeight="Medium"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Piano Roll Workspace Mock -->
        <Border Grid.Row="1" Background="#0A0E13" ClipToBounds="True">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Keyboard Gutter -->
                <Border Grid.Column="0" Background="{StaticResource Surface.Elevated}" BorderBrush="{StaticResource Border.Subtle}" BorderThickness="0,0,1,0">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="C4" Foreground="{StaticResource Text.Secondary}" FontSize="11" Margin="0,30" />
                        <TextBlock Text="C3" Foreground="{StaticResource Text.Secondary}" FontSize="11" Margin="0,30" />
                    </StackPanel>
                </Border>
                
                <!-- Timeline & Grid -->
                <Grid Grid.Column="1">
                    <!-- Measure Grid Lines (Mock) -->
                    <Path Data="M100,0 V1000 M200,0 V1000 M300,0 V1000 M400,0 V1000 M500,0 V1000 M600,0 V1000 M700,0 V1000 M800,0 V1000" Stroke="{StaticResource Border.Subtle}" StrokeThickness="1" />
                    <!-- Horizontal Lines -->
                    <Path Data="M0,40 H2000 M0,80 H2000 M0,120 H2000 M0,160 H2000 M0,200 H2000" Stroke="{StaticResource Border.Subtle}" StrokeThickness="1" Opacity="0.5" />
                    
                    <!-- Note Blocks (Mock) -->
                    <Border Background="{StaticResource Accent.Primary}" CornerRadius="2" Width="40" Height="12" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="120,44,0,0" />
                    <Border Background="{StaticResource Accent.Primary}" CornerRadius="2" Width="60" Height="12" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="180,84,0,0" />
                    <Border Background="#238636" CornerRadius="2" Width="20" Height="12" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="240,124,0,0" />
                    <Border Background="#238636" CornerRadius="2" Width="80" Height="12" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="280,164,0,0" />

                    <!-- Playhead -->
                    <Border Width="2" Background="{StaticResource Status.Error}" HorizontalAlignment="Left" Margin="100,0,0,0">
                        <Polygon Points="-5,0 7,0 1,8" Fill="{StaticResource Status.Error}" HorizontalAlignment="Center" VerticalAlignment="Top"/>
                    </Border>
                </Grid>
            </Grid>
        </Border>

        <!-- Virtual Piano Placeholder -->
        <Border Grid.Row="2" Background="{StaticResource Surface.Elevated}" BorderBrush="{StaticResource Border.Subtle}" BorderThickness="0,1,0,0">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="[ Virtual Piano Workspace ]" Foreground="{StaticResource Text.Disabled}" FontSize="14" FontWeight="Medium" />
                <TextBlock Text="Interactive 61/88 Keyboard Rendered Here" Foreground="{StaticResource Text.Disabled}" FontSize="12" Margin="0,4,0,0" HorizontalAlignment="Center" />
            </StackPanel>
        </Border>

        <!-- Transport Bar -->
        <Border Grid.Row="3" Background="{StaticResource Surface.Elevated}" BorderBrush="{StaticResource Border.Subtle}" BorderThickness="0,1,0,0" Padding="24,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="200" />
                </Grid.ColumnDefinitions>
                
                <!-- Time & Status -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="01:32" FontSize="13" Foreground="{StaticResource Text.Primary}" FontWeight="Medium" Margin="0,0,4,0"/>
                    <TextBlock Text="/ 04:46" FontSize="13" Foreground="{StaticResource Text.Secondary}" Margin="0,0,16,0"/>
                    <Border Background="#1A2D21" BorderBrush="#238636" BorderThickness="1" CornerRadius="4" Padding="6,2">
                        <TextBlock Text="Ready" FontSize="11" Foreground="#3FB950" />
                    </Border>
                </StackPanel>
                
                <!-- Playback Controls -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Button Padding="8" Margin="0,0,8,0" Background="Transparent" BorderThickness="0">
                        <Path Data="M6 6h2v12H6zm3.5 6l8.5 6V6z" Fill="{StaticResource Text.Primary}" Width="16" Height="16" Stretch="Uniform" />
                    </Button>
                    <Button Padding="16,10" Margin="0,0,8,0" Background="{StaticResource Surface.Hover}" BorderBrush="{StaticResource Border.Default}" BorderThickness="1">
                        <Path Data="M8 5v14l11-7z" Fill="{StaticResource Text.Primary}" Width="16" Height="16" Stretch="Uniform" />
                    </Button>
                    <Button Padding="8" Margin="0,0,24,0" Background="Transparent" BorderThickness="0">
                        <Path Data="M6 6h12v12H6z" Fill="{StaticResource Text.Primary}" Width="14" Height="14" Stretch="Uniform" />
                    </Button>

                    <!-- Timeline Slider -->
                    <Slider Width="300" VerticalAlignment="Center" Maximum="100" Value="32" Margin="0,0,24,0" />
                </StackPanel>
                
                <!-- Modifiers -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Margin="0,0,16,0">
                        <TextBlock Text="Speed" FontSize="12" Foreground="{StaticResource Text.Secondary}" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <ComboBox Width="70" SelectedIndex="1">
                            <ComboBoxItem Content="0.50x"/>
                            <ComboBoxItem Content="1.00x"/>
                            <ComboBoxItem Content="1.50x"/>
                        </ComboBox>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Trans" FontSize="12" Foreground="{StaticResource Text.Secondary}" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <ComboBox Width="60" SelectedIndex="1">
                            <ComboBoxItem Content="-1"/>
                            <ComboBoxItem Content="0"/>
                            <ComboBoxItem Content="+1"/>
                        </ComboBox>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
